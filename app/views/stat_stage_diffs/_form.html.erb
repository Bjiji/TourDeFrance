<%= form_for(@stat_stage_diff) do |f| %>
  <% if @stat_stage_diff.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stat_stage_diff.errors.count, "error") %> prohibited this stat_stage_diff from being saved:</h2>

      <ul>
      <% @stat_stage_diff.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
